if SHIELD_IO_DONGLE
config ZMK_KEYBOARD_NAME
	default "IO3 Dongle"
config ZMK_SPLIT_BLE_ROLE_CENTRAL
	default y
config ZMK_USB
	default y
endif

if SHIELD_IO_LEFT || SHIELD_IO_RIGHT || SHIELD_IO_DONGLE
config ZMK_SPLIT
	default y
config ZMK_BLE
	default y
endif
